# .env (LOCAL DEV ONLY)
# cara 1: export semua dari file (zsh/bash)
# set -a
# source .env
# set +a

# lalu jalankan
#mvn -DskipTests spring-boot:run

SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/taskplanner
SPRING_DATASOURCE_USERNAME=username_database
SPRING_DATASOURCE_PASSWORD=password_database
SPRING_JWT_SECRET=a_very_secret_change_me
SPRING_JWT_EXP_MS=3600000